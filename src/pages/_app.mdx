import './globals.css';

export default function App({ Component, pageProps }) {
    return (
        <main className="text-neutral-900">
            <Component {...pageProps} />
        </main>
    )
}
