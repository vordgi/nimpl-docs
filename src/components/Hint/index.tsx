const Hint: React.FC<{ children: React.ReactNode }> = ({ children }) => (
    <div className="rounded-lg border border-neutral-400 bg-neutral-100 p-3 mt-2 mb-2">
        {children}
    </div>
)

export default Hint;
